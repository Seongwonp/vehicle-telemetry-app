# Flutter/Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
*.g.dart
*.freezed.dart

# Android — 2026-09-16부터 플랫폼 설정을 추적한다(에뮬레이터 검증). 로컬 SDK 경로·빌드 산출물·서명 키는 제외한다.
android/.gradle/
android/.kotlin/
android/local.properties
android/app/build/
android/build/
android/key.properties
android/**/*.jks
android/**/*.keystore
android/app/src/main/java/io/flutter/plugins/GeneratedPluginRegistrant.java
*.apk
*.aab

# iOS
ios/
*.ipa

# macOS — 앱 설정(entitlements, Info.plist, Xcode 프로젝트)은 추적하되
# 로컬 빌드 산출물/CocoaPods 캐시는 제외한다.
macos/Flutter/ephemeral/
macos/Pods/
macos/build/

# Web
web/

# Windows
windows/

# Linux
linux/

# 환경변수 (API 주소 등)
.env
.env.*

# IDE
.idea/
.vscode/
*.iml

# 골든 비교가 실패할 때 flutter test가 만드는 차이 이미지 — 기준이 아니라 진단 산출물이다.
# (예전에 커밋된 두 파일은 추적 해제 여부를 결정해야 한다 — docs/HANDOFF_2026-09-14.md)
test/golden/failures/

# Jupyter (서버 환경 잔재)
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db
