# 인계 — 2026-09-16 (앱 저장소)

> 백엔드 저장소 인계: `../vehicle-telemetry-platform/docs/HANDOFF_2026-09-16.md` — 이번 세션 작업의 대부분은 그쪽에 있다.
> **다음 작업은 다른 PC(노트북)에서 이어질 수 있다.** `git pull --ff-only`만 하면 된다 — 미커밋 변경 없음.

## 1. 저장소 상태

| | |
| --- | --- |
| HEAD = origin/main | `3cd981a` |
| CI | success — `verify`·`golden` 둘 다 ([run](https://github.com/Seongwonp/vehicle-telemetry-app/actions/runs/35054135000)) |
| 미커밋 변경 | **없음** |

## 2. 이번에 바꾼 것 — `rpm`을 double로 (`3cd981a`)

- 백엔드 계약은 OBD-II RPM이 0.25 단위라 `Double`인데 앱 모델만 `int`로 받아 `fromJson`에서 잘랐다(`2000.25` → `2000`).
- 대시보드가 그 잘린 값을 다시 `toDouble()`로 넓혀 넘기고 있어서 **화면에는 손실이 드러나지 않았다.**
- `telemetry.dart`의 필드와 `fromJson`을 double로 바꾸고, 대시보드의 불필요한 `toDouble()`을 뺐다. **표시 동작은 그대로다.**
- 발견 경로: GitHub Copilot CLI 제3자 리뷰 → 코드로 확인. 같은 리뷰의 "앱이 JWT 대신 `******`를 보낸다"는 **사실이 아니었다**(`api_client.dart:76`은 실제 토큰을 보낸다).

**검증**: `flutter test` 256건 통과(골든 1 skip), 골든 비교 8/8, `flutter analyze` 무이슈, `dart format` 변경 0.

## 3. 결정 대기 (그대로 유지)

| 항목 | 상태 |
| --- | --- |
| **Figma 디자인 선택** | 커넥터 인증 후 파일 링크 필요. 연 파일만 평가한다. 비교 기준 HTML 시안: https://claude.ai/code/artifact/b0c97f13-95c3-4b2d-90d0-6586a636b759 |
| 한글 글꼴 앱 번들 | 보류 — 스냅샷 한글은 나눔고딕, 실기기는 OS 글꼴이라 **한글 모양·폭은 스냅샷으로 판단하지 않는다** |
| 1.5배 글자에서 차량 이름 잘림 | 디자인 비교 항목 |
| Android/iOS 플랫폼 추가·하단 탐색·별도 랜딩 | 보류 |
| 실기기 E2E | 미검증 |
| **다중 사용자** | 백엔드가 차량 소유권 검사를 REST에도 걸었다(ADR-025). 지금은 관리자 계정이라 앱 동작이 그대로지만, 다중 사용자를 도입하면 **앱의 차량 목록이 사용자별로 달라진다** |

## 4. 다음 실행 명령과 성공 기준

```bash
git pull --ff-only
flutter pub get
dart format --output=none --set-exit-if-changed lib test integration_test
flutter analyze
flutter test
flutter test test/golden --run-skipped
```

성공: format 변경 0, analyze `No issues found!`, 256건 통과(골든 1 skip), 골든 비교 `+8`,
출력에 `google_fonts was unable` 없음. 기준 이미지를 **의도적으로** 바꿀 때만 `--update-goldens`를 쓰고,
8장을 전부 열어 예상한 변경만 있는지 본 뒤 일반 비교를 다시 통과시킨다.
